# Frontend API base URL. In local development, Vite proxy is enough, so this can be left blank.
# For production, set the deployed backend URL and include the /api path if needed.
VITE_API_BASE_URL=http://localhost:5000/api
